# ERROR esperado: 'root' is not allowed at server level (only inside location)
server {
    listen 8080;
    root   ./www/html;

    location / {
        methods GET;
        index   index.html;
    }
}
