# ERROR esperado: unknown HTTP method 'BREW'
# Solo aceptamos GET, POST, DELETE.
server {
    listen 8080;

    location / {
        root    ./www/html;
        methods GET BREW;
        index   index.html;
    }
}
